// Tauri v2 serves the SvelteKit build as static HTML inside a WebView.
// Server-side rendering is not available; run everything client-side.
// See https://v2.tauri.app/start/frontend/sveltekit/
export const ssr = false;
export const prerender = true;
