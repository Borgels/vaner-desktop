{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions every Vaner window needs: invoke Rust commands, receive events, read/write clipboard, open dialogs. The companion and onboarding windows must be listed — without them, invoke() calls (including @tauri-apps/api/app getVersion) silently fail and the Diagnostics pane renders 'Desktop app: unknown'.",
  "windows": ["main", "companion", "onboarding"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-close",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text",
    "dialog:default",
    "positioner:default",
    "updater:default"
  ]
}
